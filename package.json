{
  "name": "real-time-collaborative-drawing-app-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "dev": "tsc --watch"
  },
  "author": "dhanushka gimhan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "rimraf": "^5.0.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
